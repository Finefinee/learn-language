void main() {
  List<int> scores = [50, 75, 20, 99, 20];

  // scores.add(10);
  // scores.remove(20);
  // scores.removeLast();
  print(scores.indexOf(20));
  print(scores);

  Set<String> names = {"mario", "luigi", "peach"};
}